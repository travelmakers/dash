import { createStyles } from "@travelmakers/styles";

export default createStyles((theme) => {
  return {
    root: {
      display: "flex",
      alignItems: "center",
      margin: 0,
      padding: 0,
      border: 0,
    },
  };
});
